# # spring.application.name=invoice
# # spring.profiles.active=dev
# # spring.cloud.config.uri=http://localhost:8888
# # spring.config.import=optional:configserver:
# # spring.cloud.config.fail-fast=true


# spring.application.name=invoice
# spring.profiles.active=dev

# # Esto le dice a Spring moderno que importe la configuración del servidor 8888
# spring.config.import=optional:configserver:http://localhost:8888

spring.application.name=invoice
server.port=8084

spring.datasource.url=jdbc:mysql://localhost:3306/db_AppProject?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=314159
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Desactivar cloud config para pruebas locales
spring.cloud.config.enabled=false
spring.config.import=optional:configserver:
